name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: pyproject.toml

      - name: Install ruff
        run: pip install ruff

      - name: Ruff format check
        run: ruff format --check src/ tests/

      - name: Ruff lint
        run: ruff check src/ tests/

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install -e .[dev]

      - name: Run mypy
        run: mypy

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.13", "3.14"]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache Pooch data
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/extracts
            ~/Library/Caches/extracts
            ~\AppData\Local\extracts
          key: pooch-${{ runner.os }}-${{ hashFiles('src/extracts/data/registry.txt') }}
          restore-keys: |
            pooch-${{ runner.os }}-

      - name: Install dependencies
        run: pip install -e .[dev]

      - name: Run tests with coverage
        run: pytest --cov=extracts --cov-report=term-missing --cov-report=xml

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.13'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          flags: unit

  test-min-deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install uv
        run: pip install uv

      - name: Install minimum dependency versions
        run: |
          uv pip install --system --resolution=lowest-direct -e .
          uv pip install --system pytest

      - name: Run tests
        run: pytest tests/ --ignore=tests/test_remote.py -o addopts=

  test-remote:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Cache Pooch data
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/extracts
            ~/Library/Caches/extracts
            ~\AppData\Local\extracts
          key: pooch-${{ runner.os }}-${{ hashFiles('src/extracts/data/registry.txt') }}
          restore-keys: |
            pooch-${{ runner.os }}-

      - name: Install dependencies
        run: pip install -e .[dev]

      - name: Run remote tests
        run: pytest tests/test_remote.py -v -m network -o addopts= --cov=extracts --cov-report=xml

      - name: Upload remote coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          flags: remote
